export default class AuthController {
    login(email: string, password: string): Promise<{
        accessToken: any;
    }>;
    /**
     *
     * @param email
     * @param id
     */
    private static createLoginTokens;
}
